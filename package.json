{
  "name": "github-actions-training-demo",
  "version": "1.0.0",
  "description": "Static training demo app for GitHub Actions CI/CD with AWS OIDC",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "lint": "eslint .",
    "test": "vitest --run"
  },
  "devDependencies": {
    "eslint": "^9.0.0",
    "eslint-plugin-import": "^2.29.0",
    "vite": "^5.0.0",
    "vitest": "^1.0.0"
  }
}